<div class="block">
  <div class="media">
    <div class="media-left">
      <%= image_tag @product.img(:medium) %>
    </div>
    <div class="media-body">
      <h4 class="media-heading"><%= @product.title %></h4>
      <p><%= @product.description %></p>
      <p>
        Посмотреть и приобрести товар можно по адресу ...
        Или свяжитесь с продавцом-консультантом по телефону ...
      </p>
    </div>
  </div>
  <div class="clearfix">
    <div class="pull-right">
      <%= link_to 'Редактировать', edit_product_path(@product), class: 'btn btn-primary ' if admin? %>
      <%= link_to 'Удалить', @product, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-primary ' if admin? %>
      <%= link_to 'Назад', products_path, class: 'btn btn-primary ' %>
    </div>
  </div>
</div>
<%= render 'reviews/form' if signed_in? %>

<%= render @reviews %>
<%= will_paginate @reviews, renderer: BootstrapPagination::Rails %>